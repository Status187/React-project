import React from 'react';
import logo from '../image/logo.png';

const Header = () => {
    return (
        <header className='header'>
            <img src={logo}></img>
        </header>
    )
}

export default Header;